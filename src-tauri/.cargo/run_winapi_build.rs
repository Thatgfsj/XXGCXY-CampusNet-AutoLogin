// Script to execute the winapi build.rs manually
// We can't include build.rs directly because it uses std and has main
// Instead, we'll compile and run build.rs directly
fn main() {
    println!("placeholder - this will not be used");
}
